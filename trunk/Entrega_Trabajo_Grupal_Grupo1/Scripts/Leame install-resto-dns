El comando install-resto-dns instala los archivos de configuracion necesarios para poner en funcionamiento el servidor de segundo nivel que se utiliza en las sedes de la quiaca y humahuaca.

No tiene parametros de entrada.

Utilizacion: sudo ./install-resto-dns.sh
